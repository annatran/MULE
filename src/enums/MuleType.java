package enums;

/**
 * Enum for Mule Types, which will then determine what type of property the property containing it will be.
 * 
 * @author PaulPa
 *
 */

public enum MuleType {
	NONE, BLANK, ENERGY, FOOD, SMITHORE
}
